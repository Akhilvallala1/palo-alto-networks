"""Conduit — a proxy-first AI gateway for lead-to-cash workflows.

Import the frozen interface contract from `conduit.contracts`; every component
in the system is built against those shapes.
"""

__version__ = "0.1.0"
